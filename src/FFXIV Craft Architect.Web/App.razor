<Router AppAssembly="@typeof(App).Assembly">
    <Found Context="routeData">
        <ErrorBoundary @ref="_errorBoundary">
            <ChildContent>
                <RouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)" />
                <FocusOnNavigate RouteData="@routeData" Selector="h1" />
            </ChildContent>
            <ErrorContent Context="exception">
                <MudContainer MaxWidth="MaxWidth.Medium" Class="pa-4">
                    <MudPaper Outlined="true" Class="pa-6 mt-4">
                        <MudText Typo="Typo.h4" Class="mb-2">This screen could not be loaded</MudText>
                        <MudText Typo="Typo.body1" Color="Color.Secondary" Class="mb-4">
                            Your saved plans are still on this device. Try the screen again, or return to the Recipe Planner.
                        </MudText>
                        <MudStack Row="true" Spacing="2">
                            <MudButton Variant="Variant.Filled" Color="Color.Primary" OnClick="RecoverFromError">
                                Try Again
                            </MudButton>
                            <MudButton Variant="Variant.Outlined" Color="Color.Secondary" Href="/">
                                Recipe Planner
                            </MudButton>
                        </MudStack>
                        <details class="mt-4">
                            <summary>Technical detail</summary>
                            <MudText Typo="Typo.body2" Class="mt-2">
                                @exception.GetType().Name: @exception.Message
                            </MudText>
                        </details>
                    </MudPaper>
                </MudContainer>
            </ErrorContent>
        </ErrorBoundary>
    </Found>
    <NotFound>
        <PageTitle>Not found</PageTitle>
        <LayoutView Layout="@typeof(MainLayout)">
            <MudContainer MaxWidth="MaxWidth.Medium" Class="pa-4">
                <MudPaper Outlined="true" Class="pa-6 mt-4">
                    <MudText Typo="Typo.h4" Class="mb-2">Page not found</MudText>
                    <MudText Typo="Typo.body1" Color="Color.Secondary" Class="mb-4">
                        This address does not match a Craft Architect screen.
                    </MudText>
                    <MudButton Variant="Variant.Filled" Color="Color.Primary" Href="/">
                        Return to Recipe Planner
                    </MudButton>
                </MudPaper>
            </MudContainer>
        </LayoutView>
    </NotFound>
</Router>

<EngineTransactionAcceptanceProbe />
<HostedOrderSyncSession />

@code {
    private ErrorBoundary? _errorBoundary;

    private void RecoverFromError()
    {
        _errorBoundary?.Recover();
    }
}
