<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:vm="clr-namespace:FFXIV_Craft_Architect.ViewModels"
                    xmlns:core="clr-namespace:FFXIV_Craft_Architect.Core.Models;assembly=FFXIV_Craft_Architect.Core"
                    xmlns:local="clr-namespace:FFXIV_Craft_Architect.Views.DataTemplates">
    
    <!-- ============================================================
         Market Card DataTemplates
         
         These templates replace the code-behind methods:
         - CreateExpandableMarketCard()
         - CreateCollapsedMarketCard()
         - CreateWorldOptionPanel()
         
         All visual properties match the original code-behind exactly.
         ============================================================ -->
    
    <!-- Boolean to Visibility Converter -->
    <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    
    <!-- Inverse Boolean to Visibility Converter -->
    <local:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
    
    <!-- Null to Visibility Converter -->
    <local:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    
    <!-- String NullOrEmpty to Visibility Converter -->
    <local:StringNullOrEmptyToVisibilityConverter x:Key="StringNullOrEmptyToVisibilityConverter"/>
    
    <!-- Savings Visibility Converter -->
    <local:SavingsVisibilityConverter x:Key="SavingsVisibilityConverter"/>
    
    <!-- String to Brush Converter -->
    <local:StringToBrushConverter x:Key="StringToBrushConverter"/>
    
    <!-- Boolean to FontStyle Converter -->
    <local:BoolToFontStyleConverter x:Key="BoolToFontStyleConverter"/>
    
    <!-- String to FontWeight Converter -->
    <local:StringToFontWeightConverter x:Key="StringToFontWeightConverter"/>
    
    <!-- String Equals to Visibility Converter -->
    <local:StringEqualsToVisibilityConverter x:Key="StringEqualsToVisibilityConverter"/>
    
    <!-- ============================================================
         EXPANDABLE MARKET CARD TEMPLATE
         Main template for the expandable market card in Market Logistics
         ============================================================ -->
    <DataTemplate DataType="{x:Type vm:MarketCardViewModel}">
        <Border Background="{DynamicResource Brush.Surface.Card.Market}"
                BorderBrush="{DynamicResource Brush.Border.Card.Market}"
                BorderThickness="1"
                CornerRadius="3"
                Padding="0"
                Margin="0,0,0,4">
            <StackPanel>
                <!-- Clickable Header -->
                <Border Background="{DynamicResource Brush.Surface.Card.Market.Header}"
                        CornerRadius="3"
                        Padding="8,6,8,6"
                        Cursor="Hand">
                    <Border.InputBindings>
                        <MouseBinding MouseAction="LeftClick" Command="{Binding ToggleExpandCommand}"/>
                    </Border.InputBindings>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Left Side: Item name, quantity, DC average -->
                        <StackPanel Grid.Column="0" 
                                    Orientation="Horizontal" 
                                    VerticalAlignment="Center">
                            <TextBlock Text="{Binding Name}"
                                       FontWeight="SemiBold"
                                       FontSize="12"
                                       VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Summary.QuantityDisplay}"
                                       FontSize="12"
                                       Foreground="{StaticResource LightGrayBrush}"
                                       VerticalAlignment="Center"
                                       Margin="4,0,0,0"/>
                            
                            <!-- Error display -->
                            <TextBlock Text="{Binding Error, StringFormat='  •  Error: {0}'}"
                                       Foreground="{StaticResource ErrorRedBrush}"
                                       FontSize="10"
                                       VerticalAlignment="Center"
                                       Margin="8,0,0,0"
                                       Visibility="{Binding Error, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}"/>
                            
                            <!-- DC Average (shown when no error) -->
                            <TextBlock Text="{Binding DCAveragePrice, StringFormat='  •  Avg: {0:N0}g'}"
                                       Foreground="{StaticResource GrayBrush}"
                                       FontSize="10"
                                       VerticalAlignment="Center"
                                       Margin="8,0,0,0"
                                       Visibility="{Binding Error, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                        </StackPanel>
                        
                        <!-- Right Side: Recommended world info -->
                        <StackPanel Grid.Column="1" 
                                    Orientation="Horizontal" 
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center">
                            <!-- Has options display -->
                            <StackPanel Orientation="Horizontal"
                                        Visibility="{Binding HasOptions, Converter={StaticResource BoolToVisibilityConverter}}">
                                <!-- Recommended world name -->
                                <TextBlock Text="{Binding RecommendedWorld.WorldName}"
                                           FontWeight="SemiBold"
                                           FontSize="11"
                                           Foreground="{Binding RecommendedWorld.WorldNameForeground, FallbackValue={StaticResource AccentGoldBrush}}"
                                           VerticalAlignment="Center"/>
                                
                                <!-- Blacklist button (shown if travel prohibited or congested) -->
                                <Button Content="🚫"
                                        FontSize="12"
                                        Width="24"
                                        Height="24"
                                        Padding="0"
                                        Margin="4,0,0,0"
                                        Background="Transparent"
                                        BorderBrush="{StaticResource IndianRedBrush}"
                                        BorderThickness="1"
                                        Visibility="{Binding RecommendedWorld.ShowBlacklistButton, Converter={StaticResource BoolToVisibilityConverter}}"
                                        ToolTip="{Binding RecommendedWorld.WorldName, StringFormat='Blacklist {0} - This world is not available to visit right now. Click to exclude from recommendations for 30 minutes.'}"/>
                                
                                <!-- Cost display -->
                                <TextBlock Text="{Binding RecommendedWorld.CostDisplay, StringFormat='  {0}'}"
                                           FontSize="11"
                                           VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RecommendedWorld.IsFullyUnderAverage}" Value="True">
                                                    <Setter Property="Foreground" Value="{StaticResource LightGreenBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                
                                <!-- Travel prohibited warning -->
                                <TextBlock Text="  (travel prohibited)"
                                           FontSize="9"
                                           Foreground="{StaticResource IndianRedBrush}"
                                           VerticalAlignment="Center"
                                           Visibility="{Binding RecommendedWorld.IsTravelProhibited, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            </StackPanel>
                            
                            <!-- No viable listings -->
                            <TextBlock Text="No viable listings"
                                       Foreground="{StaticResource WarningOrangeBrush}"
                                       FontSize="10"
                                       HorizontalAlignment="Right"
                                       Visibility="{Binding HasOptions, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- Expandable Content -->
                <StackPanel Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVisibilityConverter}}"
                            Margin="8,6,8,8">
                    <!-- Options Header -->
                        <TextBlock Text="{Binding OptionsHeaderText}"
                               FontWeight="SemiBold"
                               FontSize="10"
                               Margin="0,0,0,4"
                               Foreground="{StaticResource GrayBrush}"
                               Visibility="{Binding HasOptions, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    
                    <!-- World Options -->
                    <ItemsControl ItemsSource="{Binding WorldOptions}"
                                  Visibility="{Binding HasOptions, Converter={StaticResource BoolToVisibilityConverter}}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:WorldOptionViewModel}">
                                <Border Background="{Binding BackgroundColor, Converter={StaticResource StringToBrushConverter}}"
                                        CornerRadius="2"
                                        Padding="6,4,6,4"
                                        Margin="0,1,0,2"
                                        BorderBrush="{Binding BorderBrushColor, Converter={StaticResource StringToBrushConverter}, FallbackValue={x:Null}}"
                                        BorderThickness="{Binding BorderThickness}"
                                        Opacity="{Binding Opacity}">
                                    <StackPanel>
                                        <!-- World Header with Badges -->
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <!-- World Name -->
                                            <TextBlock Text="{Binding WorldName}"
                                                       FontWeight="SemiBold"
                                                       FontSize="11"
                                                       Margin="0,0,6,0"
                                                       VerticalAlignment="Center"
                                                       Foreground="{Binding WorldNameForeground, Converter={StaticResource StringToBrushConverter}}"/>
                                            
                                            <!-- Congested Badge -->
                                            <TextBlock Text="⚠ Congested"
                                                       Foreground="{StaticResource WarningOrangeBrush}"
                                                       FontSize="9"
                                                       FontWeight="Normal"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,4,0"
                                                       Visibility="{Binding ShowCongestedBadge, Converter={StaticResource BoolToVisibilityConverter}}"
                                                       ToolTip="This world is congested. Travel may be limited during peak hours."/>
                                            
                                            <!-- Travel Prohibited Badge -->
                                            <TextBlock Text="🚫 TRAVEL BLOCKED"
                                                       Foreground="{StaticResource ErrorRedBrush}"
                                                       FontSize="9"
                                                       FontWeight="Bold"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,4,0"
                                                       Visibility="{Binding ShowTravelProhibitedBadge, Converter={StaticResource BoolToVisibilityConverter}}"
                                                       ToolTip="This world is currently at capacity. Travel is temporarily prohibited. Try again in 30-60 minutes."/>
                                            
                                            <!-- Blacklisted Badge -->
                                            <TextBlock Text="⛔ BLACKLISTED"
                                                       Foreground="{StaticResource DarkGrayBrush}"
                                                       FontSize="9"
                                                       FontWeight="Bold"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,4,0"
                                                       Visibility="{Binding ShowBlacklistedBadge, Converter={StaticResource BoolToVisibilityConverter}}"
                                                       ToolTip="You have temporarily blacklisted this world. It will be excluded from recommendations."/>
                                            
                                            <!-- Recommended Star -->
                                            <TextBlock Text="★"
                                                       Foreground="{StaticResource AccentGoldBrush}"
                                                       FontSize="10"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,4,0"
                                                       Visibility="{Binding IsRecommended, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                            
                                            <!-- Competitive Checkmark -->
                                            <TextBlock Text="✓"
                                                       Foreground="{StaticResource LightGreenBrush}"
                                                       FontSize="10"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,4,0"
                                                       Visibility="{Binding IsCompetitive, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        </StackPanel>
                                        
                                        <!-- Total Cost -->
                                        <TextBlock FontSize="10" Margin="0,1,0,2">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsFullyUnderAverage}" Value="True">
                                                            <Setter Property="Foreground" Value="{StaticResource LightGreenBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                            <TextBlock.Text>
                                                <MultiBinding StringFormat="{}{0} total  •  ~{1}/ea">
                                                    <Binding Path="CostDisplay"/>
                                                    <Binding Path="PricePerUnitDisplay"/>
                                                </MultiBinding>
                                            </TextBlock.Text>
                                            <TextBlock.ToolTip>
                                                <Binding Path="HasExcess">
                                                    <Binding.Converter>
                                                        <local:BoolToStringConverter TrueValue="FFXIV requires buying full stacks. You'll have excess items." FalseValue="{x:Null}"/>
                                                    </Binding.Converter>
                                                </Binding>
                                            </TextBlock.ToolTip>
                                        </TextBlock>
                                        
                                        <!-- Best Unit Price (if competitive) -->
                                        <TextBlock Text="{Binding BestSingleListing, StringFormat='Best: {0:N0}g/ea x{1}'}"
                                                   FontSize="9"
                                                   Foreground="{StaticResource AccentGoldBrush}"
                                                   Margin="0,0,0,2"
                                                   Visibility="{Binding BestSingleListing, Converter={StaticResource NullToVisibilityConverter}}"/>
                                        
                                        <!-- Listings Header -->
                                        <TextBlock Text="Listings:"
                                                   FontSize="9"
                                                   Foreground="{StaticResource GrayBrush}"
                                                   Margin="0,2,0,1"/>
                                        
                                        <!-- Individual Listings -->
                                        <ItemsControl ItemsSource="{Binding Listings}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate DataType="{x:Type vm:ListingViewModel}">
                                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,1">
                                                        <!-- Quantity -->
                                                        <TextBlock Text="{Binding QuantityDisplay}"
                                                                   FontSize="9"
                                                                   Width="100"
                                                                   Foreground="{Binding QuantityForeground, Converter={StaticResource StringToBrushConverter}}"
                                                                   FontStyle="{Binding IsQuantityItalic, Converter={StaticResource BoolToFontStyleConverter}}"
                                                                   ToolTip="{Binding QuantityTooltip}"/>
                                                        
                                                        <!-- Price -->
                                                        <TextBlock Text="{Binding PricePerUnit, StringFormat='@{0:N0}g'}"
                                                                   FontSize="9"
                                                                   Width="60"
                                                                   Foreground="{Binding PriceForeground, Converter={StaticResource StringToBrushConverter}}"
                                                                   FontWeight="{Binding PriceFontWeight, Converter={StaticResource StringToFontWeightConverter}}"/>
                                                        
                                                        <!-- Subtotal -->
                                                        <TextBlock Text="{Binding SubtotalDisplay, StringFormat='= {0}'}"
                                                                   FontSize="9"
                                                                   Foreground="{Binding SubtotalForeground, Converter={StaticResource StringToBrushConverter}}"
                                                                   Width="70"/>
                                                        
                                                        <!-- Retainer -->
                                                        <TextBlock Text="{Binding RetainerDisplay}"
                                                                   FontSize="9"
                                                                   Foreground="{Binding RetainerForeground, Converter={StaticResource StringToBrushConverter}}"
                                                                   FontStyle="Italic"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </StackPanel>
        </Border>
    </DataTemplate>
    
    <!-- ============================================================
         COLLAPSED MARKET CARD TEMPLATE
         Compact card for split-pane view
         ============================================================ -->
    <DataTemplate x:Key="CollapsedMarketCardTemplate" DataType="{x:Type vm:MarketCardViewModel}">
        <Border Background="{DynamicResource Brush.Surface.Card.Market}"
                CornerRadius="4"
                Padding="{DynamicResource Layout.MarketCard.Collapsed.Padding}"
                Margin="{DynamicResource Layout.MarketCard.Collapsed.Margin}"
                Width="{DynamicResource Layout.MarketCard.Collapsed.Width}"
                Cursor="Hand">
            <Border.InputBindings>
                <MouseBinding MouseAction="LeftClick" Command="{Binding SelectCommand}"/>
            </Border.InputBindings>
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border.Card.Market}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                            <Setter Property="BorderBrush" Value="{DynamicResource Brush.Accent.Primary}"/>
                            <Setter Property="BorderThickness" Value="2"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="{Binding Name}"
                               FontWeight="SemiBold"
                               FontSize="12"
                               Foreground="{StaticResource Brush.Text.Primary}"
                               TextTrimming="CharacterEllipsis"/>

                    <Border Grid.Column="1"
                            Background="{StaticResource WarningOrangeBrush}"
                            CornerRadius="3"
                            Padding="4,1"
                            Margin="6,0,0,0"
                            Visibility="{Binding Plan.HasSufficientStock, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                        <TextBlock Text="{Binding Plan.StockShortfall, StringFormat='SHORT {0}'}"
                                   FontSize="9"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource Brush.Text.Primary}"/>
                    </Border>
                </Grid>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               FontSize="11"
                               Margin="0,0,6,0"
                               VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="🛒"/>
                                <Setter Property="Foreground" Value="{StaticResource Brush.Status.Info}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding RequiresSplitPurchase}" Value="True">
                                        <Setter Property="Text" Value="⇄"/>
                                        <Setter Property="Foreground" Value="{StaticResource WarningOrangeBrush}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsVendorOnly}" Value="True">
                                        <Setter Property="Text" Value="🏪"/>
                                        <Setter Property="Foreground" Value="{StaticResource AccentGoldBrush}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding RecommendedWorld.IsTravelProhibited}" Value="True">
                                        <Setter Property="Foreground" Value="{StaticResource IndianRedBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <TextBlock Grid.Column="1"
                               Text="{Binding Summary.QuantityDisplay}"
                               FontSize="10"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource Brush.Text.Secondary}"
                               Margin="0,0,8,0"
                               VerticalAlignment="Center"/>

                     <Grid Grid.Column="2" Margin="0,0,8,0">
                         <TextBlock Text="{Binding Error, StringFormat='Error: {0}'}"
                                    FontSize="10"
                                    Foreground="{StaticResource IndianRedBrush}"
                                    TextTrimming="CharacterEllipsis"
                                    Visibility="{Binding Error, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}"/>

                         <StackPanel Orientation="Horizontal"
                                     Visibility="{Binding RequiresSplitPurchase, Converter={StaticResource BoolToVisibilityConverter}}">
                             <StackPanel.ToolTip>
                                 <ToolTip>
                                     <StackPanel>
                                         <TextBlock Text="Split-world recommendation" FontWeight="Bold" FontSize="10"/>
                                         <TextBlock Text="Use Open Details in the summary pane for full breakdown." Margin="0,4,0,2" FontSize="10"/>
                                         <ItemsControl ItemsSource="{Binding SplitWorlds}" Margin="8,0,0,0">
                                             <ItemsControl.ItemTemplate>
                                                 <DataTemplate>
                                                     <TextBlock FontSize="10">
                                                         <Run Text="{Binding WorldDisplayName}"/>
                                                         <Run Text=": ×"/>
                                                         <Run Text="{Binding QuantityToBuy}"/>
                                                         <Run Text=" @ "/>
                                                         <Run Text="{Binding EffectivePriceDisplay}"/>
                                                         <Run Text=" = "/>
                                                         <Run Text="{Binding TotalCost, StringFormat='{}{0:N0}g'}"/>
                                                     </TextBlock>
                                                 </DataTemplate>
                                             </ItemsControl.ItemTemplate>
                                         </ItemsControl>
                                     </StackPanel>
                                 </ToolTip>
                             </StackPanel.ToolTip>
                             <TextBlock Text="⇄" FontSize="10" Foreground="{StaticResource WarningOrangeBrush}" VerticalAlignment="Center"/>
                             <TextBlock Text="{Binding SplitWorlds.Count, StringFormat=' {0} worlds'}"
                                        FontSize="10"
                                        FontWeight="SemiBold"
                                        Foreground="{StaticResource WarningOrangeBrush}"
                                        VerticalAlignment="Center"/>
                         </StackPanel>

                          <TextBlock Text="{Binding RecommendedSourceName}"
                                    FontSize="10"
                                    FontWeight="SemiBold"
                                    TextTrimming="CharacterEllipsis"
                                    Visibility="{Binding RecommendedWorld, Converter={StaticResource NullToVisibilityConverter}}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource LightGreenBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding RequiresSplitPurchase}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding RecommendedWorld.IsTravelProhibited}" Value="True">
                                            <Setter Property="Foreground" Value="{StaticResource IndianRedBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding RecommendedWorld.ShowCongestedBadge}" Value="True">
                                            <Setter Property="Foreground" Value="{StaticResource WarningOrangeBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <TextBlock Text="No viable listings"
                                   FontSize="10"
                                   Foreground="{StaticResource WarningOrangeBrush}"
                                   TextTrimming="CharacterEllipsis"
                                   Visibility="{Binding HasOptions, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                    </Grid>

                    <StackPanel Grid.Column="3" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding Summary.TotalCost, StringFormat='{}{0:N0}g'}"
                                   FontSize="10"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource WarningOrangeBrush}"
                                   HorizontalAlignment="Right"
                                   Visibility="{Binding RequiresSplitPurchase, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <TextBlock Text="{Binding Summary.TotalCost, StringFormat='{}{0:N0}g'}"
                                   FontSize="10"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource AccentGoldBrush}"
                                   HorizontalAlignment="Right"
                                   Visibility="{Binding RecommendedWorld, Converter={StaticResource NullToVisibilityConverter}}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding RequiresSplitPurchase}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <TextBlock Text="{Binding Summary.AveragePricePerUnit, StringFormat='{}{0:N0}g/ea'}"
                                   FontSize="9"
                                   Foreground="{StaticResource Brush.Text.Muted}"
                                   HorizontalAlignment="Right"
                                   Visibility="{Binding RecommendedWorld, Converter={StaticResource NullToVisibilityConverter}}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding RequiresSplitPurchase}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </DataTemplate>
    
    <!-- ============================================================
         EXPANDED PANEL TEMPLATE
         Template for the expanded panel in split-pane market view
         Replaces BuildExpandedPanel() and CreateExpandedWorldPanel()
         ============================================================ -->
    <DataTemplate DataType="{x:Type vm:ExpandedPanelViewModel}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header with Close Button -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0"
                           Text="{Binding HeaderText}"
                           FontWeight="Bold"
                           FontSize="14"
                           Foreground="{StaticResource AccentGoldBrush}"/>
                
                <Button Grid.Column="1"
                        Content="Open Details"
                        FontSize="10"
                        Padding="10,4"
                        Margin="0,0,8,0"
                        Command="{Binding OpenDetailsCommand}"
                        Background="{StaticResource CardBackgroundBrush}"
                        Foreground="{StaticResource TextPrimaryBrush}"
                        BorderBrush="{StaticResource Brush.Border.Default}"
                        BorderThickness="1"
                        Cursor="Hand"/>

                <TextBlock Grid.Column="2"
                            Text="✕"
                            FontSize="14"
                            Foreground="{StaticResource GrayBrush}"
                            Cursor="Hand"
                           ToolTip="Close">
                    <TextBlock.InputBindings>
                        <MouseBinding MouseAction="LeftClick" Command="{Binding CloseCommand}"/>
                    </TextBlock.InputBindings>
                </TextBlock>
            </Grid>
            
            <!-- DC Average -->
            <TextBlock Grid.Row="1"
                       Text="{Binding DCAverageText}"
                       FontSize="11"
                       Foreground="{StaticResource GrayBrush}"
                       Margin="0,4,0,12"/>
            
            <!-- Vendor Details -->
            <Border Grid.Row="2"
                    Background="{StaticResource CardBackgroundVendorBrush}"
                    BorderBrush="{StaticResource Brush.Border.Section.Vendor}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="10,8"
                    Margin="0,0,0,8"
                    Visibility="{Binding IsVendorMode, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel>
                    <TextBlock Text="Vendor Purchase Details"
                               FontWeight="SemiBold"
                               FontSize="11"
                               Foreground="{StaticResource GrayBrush}"/>

                    <TextBlock Text="{Binding PrimaryVendor.Name}"
                               FontWeight="SemiBold"
                               FontSize="12"
                               Foreground="{StaticResource Brush.Text.Vendor}"
                               Margin="0,4,0,0"
                               Visibility="{Binding HasVendors, Converter={StaticResource BoolToVisibilityConverter}}"/>

                    <TextBlock Text="{Binding PrimaryVendor.Location}"
                               FontSize="10"
                               Foreground="{StaticResource LightGrayBrush}"
                               Margin="0,2,0,0"
                               Visibility="{Binding HasVendors, Converter={StaticResource BoolToVisibilityConverter}}"/>

                    <TextBlock Text="{Binding PrimaryVendor.CoordinatesDisplay}"
                               FontSize="10"
                               Foreground="{StaticResource LightSkyBlueBrush}"
                               Margin="0,2,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding PrimaryVendor.HasCoordinates}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <TextBlock Text="{Binding VendorPricingText}"
                               FontSize="11"
                               Foreground="{StaticResource LightGreenBrush}"
                               Margin="0,6,0,0"/>

                    <TextBlock Text="Alternative Vendors"
                               FontWeight="SemiBold"
                               FontSize="10"
                               Foreground="{StaticResource GrayBrush}"
                               Margin="0,8,0,4"
                               Visibility="{Binding HasAlternativeVendors, Converter={StaticResource BoolToVisibilityConverter}}"/>

                    <ItemsControl ItemsSource="{Binding AlternativeVendors}"
                                  Visibility="{Binding HasAlternativeVendors, Converter={StaticResource BoolToVisibilityConverter}}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type core:VendorInfo}">
                                <Border Background="{StaticResource Brush.Surface.Section.Vendor}"
                                        BorderBrush="{StaticResource Brush.Border.Section.Vendor}"
                                        BorderThickness="1"
                                        CornerRadius="3"
                                        Padding="8,6"
                                        Margin="0,0,0,6">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}"
                                                   FontWeight="SemiBold"
                                                   FontSize="11"
                                                   Foreground="{StaticResource Brush.Text.Vendor}"/>
                                        <TextBlock Text="{Binding Location}"
                                                   FontSize="10"
                                                   Foreground="{StaticResource GrayBrush}"
                                                   Margin="0,2,0,0"/>
                                        <TextBlock Text="{Binding CoordinatesDisplay}"
                                                   FontSize="10"
                                                   Foreground="{StaticResource LightSkyBlueBrush}"
                                                   Margin="0,2,0,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding HasCoordinates}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock Text="{Binding Price, StringFormat='{}{0:N0}g each'}"
                                                   FontSize="10"
                                                   Foreground="{StaticResource LightGreenBrush}"
                                                   Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- Recommendation Details (split-specific or all-world fallback) -->
            <Grid Grid.Row="2">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ShowSingleWorldOptions}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding HasSplitWorldOptions}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsVendorMode}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <!-- Split-world recommended details -->
                <Grid Visibility="{Binding HasSplitWorldOptions, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="{Binding OptionsHeaderText}"
                               FontWeight="SemiBold"
                               FontSize="11"
                               Foreground="{StaticResource GrayBrush}"
                               Margin="0,0,0,8"/>

                    <ItemsControl Grid.Row="1" ItemsSource="{Binding SplitWorlds}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:ExpandedSplitWorldViewModel}">
                                    <Border CornerRadius="3"
                                            Padding="8,6,8,6"
                                            Margin="0,0,0,6">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{StaticResource WorldBackgroundDefaultBrush}"/>
                                                <Setter Property="BorderBrush" Value="{x:Null}"/>
                                                <Setter Property="BorderThickness" Value="0"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsCongested}" Value="True">
                                                        <Setter Property="BorderBrush" Value="{StaticResource WarningOrangeBrush}"/>
                                                        <Setter Property="BorderThickness" Value="1"/>
                                                    </DataTrigger>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding IsPrimary}" Value="True"/>
                                                            <Condition Binding="{Binding IsHomeWorld}" Value="False"/>
                                                            <Condition Binding="{Binding IsCongested}" Value="False"/>
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Background" Value="{StaticResource WorldBackgroundRecommendedBrush}"/>
                                                        <Setter Property="BorderBrush" Value="{StaticResource SuccessGreenBrush}"/>
                                                        <Setter Property="BorderThickness" Value="1"/>
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel>
                                            <Grid Cursor="Hand">
                                                <Grid.InputBindings>
                                                    <MouseBinding MouseAction="LeftClick" Command="{Binding ToggleExpandCommand}"/>
                                                </Grid.InputBindings>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding WorldDisplayName}" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                    <TextBlock Text="  ⚠ Congested"
                                                               Foreground="{StaticResource WarningOrangeBrush}"
                                                               FontSize="10"
                                                               FontWeight="Normal"
                                                               Visibility="{Binding ShowCongestedBadge, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                    <TextBlock Text="  🚫 TRAVEL BLOCKED"
                                                               Foreground="{StaticResource ErrorRedBrush}"
                                                               FontSize="10"
                                                               FontWeight="Bold"
                                                               Visibility="{Binding ShowTravelProhibitedBadge, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                </StackPanel>

                                                <TextBlock Grid.Column="1" Margin="0,0,8,0" Foreground="{StaticResource GrayBrush}">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Text" Value="Expand"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsExpanded}" Value="True">
                                                                    <Setter Property="Text" Value="Collapse"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>

                                                <Border Grid.Column="2"
                                                        CornerRadius="2"
                                                        Padding="6,1">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Background" Value="{StaticResource WarningOrangeBrush}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsPrimary}" Value="True">
                                                                    <Setter Property="Background" Value="{StaticResource SuccessGreenBrush}"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding IsConsolidated}" Value="True">
                                                                    <Setter Property="Background" Value="{StaticResource LightSkyBlueBrush}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock Text="{Binding ContextBadge}" FontSize="9" FontWeight="Bold" Foreground="White"/>
                                                </Border>
                                            </Grid>

                                            <TextBlock Text="{Binding QuantityDisplay}" FontSize="11" Foreground="{StaticResource AccentGoldBrush}" Margin="0,2,0,0"/>
                                            <TextBlock Text="{Binding CostText}" FontSize="11" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,2,0,0"/>
                                            <TextBlock Text="{Binding ExcessText}"
                                                       FontSize="10"
                                                       Foreground="{StaticResource WarningOrangeBrush}"
                                                       Margin="0,2,0,0"
                                                       Visibility="{Binding HasExcess, Converter={StaticResource BoolToVisibilityConverter}}"/>

                                            <Border Background="{StaticResource Brush.Surface.Listings}" CornerRadius="2" Padding="6" Margin="0,6,0,0">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <MultiDataTrigger>
                                                                <MultiDataTrigger.Conditions>
                                                                    <Condition Binding="{Binding HasListings}" Value="True"/>
                                                                    <Condition Binding="{Binding IsExpanded}" Value="True"/>
                                                                </MultiDataTrigger.Conditions>
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </MultiDataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>

                                                <ItemsControl ItemsSource="{Binding DisplayListings}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate DataType="{x:Type vm:ExpandedListingViewModel}">
                                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,1">
                                                                <TextBlock Text="{Binding QuantityDisplay}"
                                                                           FontSize="10"
                                                                           Width="130"
                                                                           Foreground="{Binding QuantityForeground, Converter={StaticResource StringToBrushConverter}}"
                                                                           FontStyle="{Binding IsQuantityItalic, Converter={StaticResource BoolToFontStyleConverter}}"/>
                                                                <TextBlock Text="{Binding PricePerUnit, StringFormat='@{0:N0}g'}"
                                                                           FontSize="10"
                                                                           Width="70"
                                                                           Foreground="{Binding PriceForeground, Converter={StaticResource StringToBrushConverter}}"
                                                                           FontWeight="{Binding PriceFontWeight, Converter={StaticResource StringToFontWeightConverter}}"/>
                                                                <TextBlock Text="{Binding SubtotalDisplay, StringFormat='= {0}'}"
                                                                           FontSize="10"
                                                                           Foreground="{Binding SubtotalForeground, Converter={StaticResource StringToBrushConverter}}"
                                                                           Width="75"/>
                                                                <TextBlock Text="{Binding RetainerDisplay}"
                                                                           FontSize="10"
                                                                           Foreground="{Binding RetainerForeground, Converter={StaticResource StringToBrushConverter}}"
                                                                           FontStyle="Italic"/>
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </Border>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                </Grid>

                <!-- Standard all-world comparison -->
                <Grid Visibility="{Binding ShowSingleWorldOptions, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="{Binding OptionsHeaderText}"
                               FontWeight="SemiBold"
                               FontSize="11"
                               Foreground="{StaticResource GrayBrush}"
                               Margin="0,0,0,8"/>

                    <ItemsControl Grid.Row="1" ItemsSource="{Binding WorldOptions}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:ExpandedWorldViewModel}">
                                    <Border Background="{Binding BackgroundColor, Converter={StaticResource StringToBrushConverter}}"
                                            CornerRadius="3"
                                            Padding="8,6,8,6"
                                            Margin="0,0,0,6"
                                            BorderBrush="{Binding BorderBrushColor, Converter={StaticResource StringToBrushConverter}, FallbackValue={x:Null}}"
                                            BorderThickness="{Binding BorderThickness}"
                                            Opacity="{Binding Opacity}">
                                        <StackPanel>
                                            <Grid Cursor="Hand">
                                                <Grid.InputBindings>
                                                    <MouseBinding MouseAction="LeftClick" Command="{Binding ToggleExpandCommand}"/>
                                                </Grid.InputBindings>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding WorldName}"
                                                           FontWeight="SemiBold"
                                                           FontSize="12"
                                                           Foreground="{Binding WorldNameForeground, Converter={StaticResource StringToBrushConverter}}"/>
                                                <TextBlock Text="  ⚠ Congested"
                                                           Foreground="{StaticResource WarningOrangeBrush}"
                                                           FontSize="10"
                                                           FontWeight="Normal"
                                                           Visibility="{Binding ShowCongestedBadge, Converter={StaticResource BoolToVisibilityConverter}}"
                                                           ToolTip="This world is congested. Travel may be limited during peak hours."/>
                                                <Button Content="🚫"
                                                        FontSize="12"
                                                        Width="24"
                                                        Height="24"
                                                        Padding="0"
                                                        Margin="4,0,0,0"
                                                        Background="Transparent"
                                                        BorderBrush="{StaticResource IndianRedBrush}"
                                                        BorderThickness="1"
                                                        Visibility="{Binding ShowBlacklistButton, Converter={StaticResource BoolToVisibilityConverter}}"
                                                        ToolTip="{Binding WorldName, StringFormat='Blacklist {0} - This world is not available to visit right now. Click to exclude from recommendations for 30 minutes.'}"/>
                                                <TextBlock Text="  🚫 TRAVEL BLOCKED"
                                                           Foreground="{StaticResource ErrorRedBrush}"
                                                           FontSize="10"
                                                           FontWeight="Bold"
                                                           Visibility="{Binding ShowTravelProhibitedBadge, Converter={StaticResource BoolToVisibilityConverter}}"
                                                           ToolTip="This world is currently at capacity. Travel is temporarily prohibited."/>
                                                <TextBlock Text="  ⛔ BLACKLISTED"
                                                           Foreground="{StaticResource DarkGrayBrush}"
                                                           FontSize="10"
                                                           FontWeight="Bold"
                                                           Visibility="{Binding ShowBlacklistedBadge, Converter={StaticResource BoolToVisibilityConverter}}"
                                                           ToolTip="You have temporarily blacklisted this world."/>
                                                <TextBlock Text="  ★ Recommended"
                                                           Foreground="{StaticResource AccentGoldBrush}"
                                                           FontSize="10"
                                                           Margin="4,0,0,0"
                                                           Visibility="{Binding IsRecommended, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                </StackPanel>

                                                <TextBlock Grid.Column="1" Foreground="{StaticResource GrayBrush}">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Text" Value="Expand"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsExpanded}" Value="True">
                                                                    <Setter Property="Text" Value="Collapse"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Grid>

                                            <TextBlock Text="{Binding CostText}" FontSize="11" Margin="0,2,0,4" ToolTip="{Binding CostTooltip}">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsFullyUnderAverage}" Value="True">
                                                                <Setter Property="Foreground" Value="{StaticResource LightGreenBrush}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>

                                            <Border Background="{StaticResource Brush.Surface.Listings}" CornerRadius="2" Padding="6" Margin="0,6,0,0">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsExpanded}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>

                                                <StackPanel>
                                                    <TextBlock Text="Listings:" FontSize="10" Foreground="{StaticResource GrayBrush}" Margin="0,0,0,2"/>

                                                    <ItemsControl ItemsSource="{Binding DisplayListings}">
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate DataType="{x:Type vm:ExpandedListingViewModel}">
                                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,1">
                                                                    <TextBlock Text="{Binding QuantityDisplay}"
                                                                               FontSize="10"
                                                                               Width="90"
                                                                               Foreground="{Binding QuantityForeground, Converter={StaticResource StringToBrushConverter}}"
                                                                               FontStyle="{Binding IsQuantityItalic, Converter={StaticResource BoolToFontStyleConverter}}"/>
                                                                    <TextBlock Text="{Binding PricePerUnit, StringFormat='@{0:N0}g'}"
                                                                               FontSize="10"
                                                                               Width="65"
                                                                               Foreground="{Binding PriceForeground, Converter={StaticResource StringToBrushConverter}}"
                                                                               FontWeight="{Binding PriceFontWeight, Converter={StaticResource StringToFontWeightConverter}}"/>
                                                                    <TextBlock Text="{Binding SubtotalDisplay, StringFormat='= {0}'}"
                                                                               FontSize="10"
                                                                               Foreground="{Binding SubtotalForeground, Converter={StaticResource StringToBrushConverter}}"
                                                                               Width="75"/>
                                                                    <TextBlock Text="{Binding RetainerDisplay}"
                                                                               FontSize="10"
                                                                               Foreground="{Binding RetainerForeground, Converter={StaticResource StringToBrushConverter}}"
                                                                               FontStyle="Italic"/>
                                                                </StackPanel>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </StackPanel>
                                            </Border>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                </Grid>
            </Grid>
            
            <!-- Vendor Information Section (shown when HasVendors is true) -->
            <StackPanel Grid.Row="3"
                        Margin="0,12,0,8">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasVendors}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsVendorMode}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <TextBlock Text="Other Vendor Sources:"
                           FontWeight="SemiBold"
                           FontSize="11"
                           Foreground="{StaticResource GrayBrush}"
                           Margin="0,0,0,8"/>
                
                <ItemsControl ItemsSource="{Binding Vendors}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type core:VendorInfo}">
                            <Border Background="{StaticResource CardBackgroundVendorBrush}"
                                    CornerRadius="3"
                                    Padding="8,6,8,6"
                                    Margin="0,0,0,6">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <!-- Vendor Icon -->
                                        <TextBlock Text="🏪"
                                                   FontSize="12"
                                                   Margin="0,0,6,0"
                                                   VerticalAlignment="Center"/>
                                        <!-- Vendor Name -->
                                        <TextBlock Text="{Binding Name}"
                                                   FontWeight="SemiBold"
                                                   FontSize="12"
                                                   Foreground="{StaticResource Brush.Text.Vendor}"
                                                   VerticalAlignment="Center"/>
                                    </StackPanel>
                                    
                                    <!-- Location -->
                                    <TextBlock Text="{Binding Location}"
                                               FontSize="10"
                                               Foreground="{StaticResource GrayBrush}"
                                               Margin="0,2,0,0"
                                               Visibility="{Binding Location, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}"/>
                                    
                                    <!-- Price -->
                                    <TextBlock FontSize="11"
                                               Foreground="{StaticResource LightGreenBrush}"
                                               Margin="0,2,0,0">
                                        <TextBlock.Text>
                                            <MultiBinding StringFormat="{}{0:N0}g each">
                                                <Binding Path="Price"/>
                                            </MultiBinding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
            
            <!-- Error Display -->
            <TextBlock Grid.Row="4"
                       Text="{Binding Error, StringFormat='Error: {0}'}"
                       FontSize="11"
                       Foreground="{StaticResource IndianRedBrush}"
                       Margin="0,8,0,0"
                       Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}"/>
            
            <!-- No Data Message -->
            <TextBlock Grid.Row="5"
                       Text="No market data available for this item"
                       FontSize="11"
                       Foreground="{StaticResource WarningOrangeBrush}"
                       Margin="0,8,0,0"
                       Visibility="{Binding ShowNoData, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </Grid>
    </DataTemplate>
    
</ResourceDictionary>
