<Window x:Class="FFXIV_Craft_Architect.CacheDiagnosticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:helpers="clr-namespace:FFXIV_Craft_Architect.Helpers"
        Title="Cache Diagnostics"
        Width="900"
        Height="700"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource WindowBackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="200" />
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource CardBackgroundBrush}" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" 
                           Text="Market Cache Diagnostics" 
                           FontSize="16" FontWeight="SemiBold" 
                           Foreground="{StaticResource TextPrimaryBrush}"/>
                
                <ui:Button Grid.Column="1"
                           Content="Close"
                           MinWidth="80"
                           MinHeight="28"
                           Appearance="Secondary"
                           Padding="12,4"
                           Click="OnCloseClick"
                           helpers:ButtonAutoShrinkBehavior.EnableAutoShrink="True"/>
            </Grid>
        </Border>
        
        <!-- Stats bar -->
        <Border Grid.Row="1" Background="{StaticResource PanelBackgroundBrush}" Padding="8">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="EntriesCount" Text="Entries: 0" Foreground="{StaticResource GrayBrush}" Margin="0,0,16,0"/>
                <TextBlock x:Name="DbSizeText" Text="DB: 0 bytes" Foreground="{StaticResource GrayBrush}" Margin="0,0,16,0"/>
                <TextBlock x:Name="WalSizeText" Text="WAL: 0 bytes" Foreground="{StaticResource AccentGoldBrush}" Margin="0,0,16,0"/>
                <TextBlock x:Name="OldestText" Text="Oldest: -" Foreground="{StaticResource GrayBrush}" Margin="0,0,16,0"/>
                <TextBlock x:Name="NewestText" Text="Newest: -" Foreground="{StaticResource GrayBrush}"/>
            </StackPanel>
        </Border>
        
        <!-- Data Grid -->
        <Border Grid.Row="2" Background="{StaticResource WindowBackgroundBrush}" Margin="0">
            <DataGrid x:Name="CacheDataGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      Background="{StaticResource WindowBackgroundBrush}"
                      RowBackground="{StaticResource PanelBackgroundBrush}"
                      AlternatingRowBackground="{StaticResource CardBackgroundAltBrush}"
                      BorderThickness="0"
                      CanUserSortColumns="True"
                      SelectionChanged="OnSelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Item ID" Binding="{Binding ItemId}" Width="80">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    
                    <DataGridTextColumn Header="DC" Binding="{Binding DataCenter}" Width="80">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource GrayBrush}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    
                    <DataGridTextColumn Header="Fetched" Binding="{Binding FetchedAt}" Width="130">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource GrayBrush}"/>
                                <Setter Property="FontSize" Value="10"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    
                    <DataGridTextColumn Header="Age" Binding="{Binding AgeText}" Width="80">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource Brush.Status.Info}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    
                    <DataGridTextColumn Header="DC Avg" Binding="{Binding DCAveragePrice}" Width="90">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource AccentGoldBrush}"/>
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    
                    <DataGridTextColumn Header="Worlds" Binding="{Binding WorldsCount}" Width="60">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource GrayBrush}"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    
                    <DataGridTextColumn Header="Listings" Binding="{Binding ListingsCount}" Width="60">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource GrayBrush}"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    
                    <DataGridTextColumn Header="Size" Binding="{Binding DataSizeText}" Width="70">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource GrayBrush}"/>
                                <Setter Property="FontSize" Value="10"/>
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
        
        <!-- Details and Actions -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="280"/>
            </Grid.ColumnDefinitions>
            
            <!-- Details Panel -->
            <Border Grid.Column="0" Background="{StaticResource PanelBackgroundBrush}" BorderBrush="{StaticResource MutedAccentBrush}" BorderThickness="0,1,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" 
                               x:Name="DetailsHeader"
                               Text="Entry Details (select an item)" 
                               FontWeight="SemiBold" 
                               Foreground="{StaticResource TextPrimaryBrush}"
                               Padding="8,4"
                               Background="{StaticResource CardBackgroundBrush}"/>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="8">
                        <TextBlock x:Name="DetailsText" 
                                   FontFamily="Consolas" 
                                   FontSize="11"
                                   TextWrapping="Wrap" 
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   Text="No item selected"/>
                    </ScrollViewer>
                </Grid>
            </Border>
            
            <!-- Actions Panel -->
            <Border Grid.Column="1" Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource MutedAccentBrush}" BorderThickness="0,1,0,0" Padding="12">
                <StackPanel>
                    <TextBlock Text="Database Path:" Foreground="{StaticResource GrayBrush}" FontSize="10" Margin="0,0,0,4"/>
                    <TextBlock x:Name="DbPathText" Text="-" Foreground="{StaticResource TextPrimaryBrush}" FontSize="10" TextWrapping="Wrap" Margin="0,0,0,12"/>
                    
                    <TextBlock Text="Actions:" Foreground="{StaticResource GrayBrush}" FontSize="10" Margin="0,0,0,8"/>
                    
                    <ui:Button Content="Refresh" MinHeight="28" Appearance="Secondary" Padding="12,4" Margin="0,0,0,8" Click="OnRefreshClick" helpers:ButtonAutoShrinkBehavior.EnableAutoShrink="True"/>
                    <ui:Button Content="Force Checkpoint (WAL)" MinHeight="28" Appearance="Secondary" Padding="12,4" Margin="0,0,0,8" Click="OnCheckpointClick" helpers:ButtonAutoShrinkBehavior.EnableAutoShrink="True"/>
                    <ui:Button Content="Vacuum Database" MinHeight="28" Appearance="Secondary" Padding="12,4" Margin="0,0,0,8" Click="OnVacuumClick" helpers:ButtonAutoShrinkBehavior.EnableAutoShrink="True"/>
                    <ui:Button Content="Clear Cache" MinHeight="28" Appearance="Danger" Padding="12,4" Margin="0,0,0,0" Click="OnClearCacheClick" helpers:ButtonAutoShrinkBehavior.EnableAutoShrink="True"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
