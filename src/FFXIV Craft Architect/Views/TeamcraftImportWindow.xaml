<Window x:Class="FFXIV_Craft_Architect.TeamcraftImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Import from Teamcraft"
        Width="500"
        Height="550"
        WindowStartupLocation="CenterOwner"
        Background="#1e1e1e">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Import from Teamcraft"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,8"/>
        
        <TextBlock Grid.Row="1"
                   Text="Paste items from Teamcraft's 'Copy as Text' feature"
                   Foreground="Gray"
                   Margin="0,0,0,16"
                   TextWrapping="Wrap"/>

        <!-- Pre-craft Items -->
        <Border Grid.Row="2"
                Background="#2d2d2d"
                CornerRadius="4"
                Padding="8"
                Margin="0,0,0,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0"
                           Text="Pre-craft Items (Optional)"
                           FontWeight="SemiBold"
                           Margin="0,0,0,4"/>
                
                <TextBox Grid.Row="1"
                         x:Name="PrecraftTextBox"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         Background="Transparent"
                         BorderThickness="1"
                         Padding="4"/>
            </Grid>
        </Border>

        <!-- Final Items -->
        <Border Grid.Row="3"
                Background="#2d2d2d"
                CornerRadius="4"
                Padding="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0"
                           Text="Final Items (Required)"
                           FontWeight="SemiBold"
                           Margin="0,0,0,4"/>
                
                <TextBox Grid.Row="1"
                         x:Name="FinalItemsTextBox"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         Background="Transparent"
                         BorderThickness="1"
                         Padding="4"/>
            </Grid>
        </Border>

        <!-- Buttons -->
        <Grid Grid.Row="4" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0"
                       x:Name="StatusText"
                       Text=""
                       Foreground="Gray"
                       VerticalAlignment="Center"/>
            
            <ui:Button Grid.Column="1"
                       Content="Cancel"
                       Appearance="Secondary"
                       Click="OnCancel"/>
            
            <ui:Button Grid.Column="3"
                       Content="Import"
                       Appearance="Primary"
                       Click="OnImport"/>
        </Grid>
    </Grid>
</Window>
