<UserControl x:Class="FFXIV_Craft_Architect.Views.Modules.ProcurementPlannerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border x:Name="ProcurementPlannerContent"
            Background="Transparent"
            Padding="8,8,16,16"
            Visibility="Collapsed">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="Procurement Plan"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource Brush.Accent.Primary}"
                       Margin="0,0,0,8"/>

            <Border Grid.Row="1"
                    Background="{StaticResource Brush.Surface.Card}"
                    CornerRadius="4"
                    Padding="12">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="ProcurementPlanPanel">
                        <TextBlock Text="No procurement plan available - fetch market data to generate actionable plan"
                                   Foreground="{StaticResource Brush.Text.Secondary}"
                                   FontSize="12"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Border>
</UserControl>
