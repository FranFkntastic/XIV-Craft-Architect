<UserControl x:Class="FFXIV_Craft_Architect.Views.Modules.AcquisitionEvaluationSidebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:helpers="clr-namespace:FFXIV_Craft_Architect.Helpers">
    <Border Background="Transparent">
        <StackPanel x:Name="AcquisitionEvaluationLeftPanel">
            <TextBlock Text="Acquisition Evaluation"
                       FontSize="18"
                       FontWeight="Bold"
                       Foreground="{StaticResource AccentGoldBrush}"
                       Margin="0,0,0,16"/>

            <ComboBox x:Name="AcquisitionFilterCombo"
                      ItemsSource="{Binding DataContext.AcquisitionEvaluation.FilterOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                      DisplayMemberPath="DisplayName"
                      SelectedValuePath="Filter"
                      SelectedValue="{Binding DataContext.AcquisitionEvaluation.CurrentFilter, RelativeSource={RelativeSource AncestorType=Window}, Mode=TwoWay}"
                      Margin="0,0,0,12">
            </ComboBox>

            <ui:Button x:Name="RefreshAcquisitionEvaluationButton"
                       Content="Refresh Evaluation"
                       Command="{Binding DataContext.AcquisitionEvaluation.RefreshCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                       Width="220"
                       HorizontalAlignment="Left"
                       helpers:ButtonAutoShrinkBehavior.EnableAutoShrink="True"/>
        </StackPanel>
    </Border>
</UserControl>
